import { createHash } from "node:crypto";
import { readFile, writeFile } from "node:fs/promises";
import { resolve } from "node:path";
import { CatalogSeedWriter } from "../src/server/catalog/seed-writer.ts";
import { createCatalogMigrationValidationReport, type CatalogSeedBundle } from "../src/server/catalog/seed-contract.ts";
import { assertLocalCatalogPublishTarget, LOCAL_CATALOG_PUBLISH_CONFIRMATION } from "../src/server/catalog/local-publish-safety.ts";
import { createPostgresPool, createTransactionalSqlClient } from "../src/server/db/postgres-client.ts";

const options=new Map(process.argv.slice(2).map((arg)=>{const at=arg.indexOf("=");if(!arg.startsWith("--")||at<0)throw new Error(`Option requires value: ${arg}`);return[arg.slice(2,at),arg.slice(at+1)];})),required=(name:string)=>{const value=options.get(name)?.trim();if(!value)throw new Error(`Missing --${name}=...`);return value;};
const approvedHash=required("review-hash"),reviewReference=required("review"),approvedAt=required("approved-at"),confirmation=required("confirm");if(!/^[a-f0-9]{64}$/.test(approvedHash)||Number.isNaN(Date.parse(approvedAt))||confirmation!==LOCAL_CATALOG_PUBLISH_CONFIRMATION)throw new Error("Invalid Jilin cleanup approval parameters.");
const root=process.cwd(),paths={candidate:resolve(root,"seeds/catalog.jilin-cleanup.draft.json"),validation:resolve(root,"seeds/catalog.jilin-cleanup.validation.json"),review:resolve(root,"seeds/catalog.jilin-cleanup.review.json"),output:resolve(root,"seeds/catalog.jilin-cleanup.approved.local.json"),approval:resolve(root,"seeds/catalog.jilin-cleanup.approval.json"),state:resolve(root,".cuac-local/runtime.json")},parse=async(path:string)=>JSON.parse(await readFile(path,"utf8")),sha=(value:string)=>createHash("sha256").update(value).digest("hex");
const candidateText=await readFile(paths.candidate,"utf8"),candidate=JSON.parse(candidateText) as CatalogSeedBundle,[validation,review,state]=await Promise.all([parse(paths.validation),parse(paths.review),parse(paths.state)]),{reviewHash,requiredApproval,...reviewBase}=review;if(reviewHash!==approvedHash||sha(JSON.stringify(reviewBase))!==approvedHash||requiredApproval!==reviewReference||review.status!=="requires_explicit_approval"||review.scope?.schoolOverwriteCount!==1||review.scope?.archiveProgramAliasCount!==3||review.scope?.archiveScholarshipAliasCount!==1||review.scope?.verifiedProgramMutationCount!==0||review.scope?.verifiedScholarshipMutationCount!==0||review.reconciliation?.destructiveDeletion!==false||review.sensitiveDataCheck?.result!=="pass")throw new Error("Approved Jilin cleanup review does not match stored operation.");
const fresh=createCatalogMigrationValidationReport(candidate);if(!fresh.ok||!validation.ok||sha(candidateText)!==review.candidateSha256||fresh.bundleSha256!==review.candidateBundleSha256||fresh.bundleSha256!==validation.bundleSha256||fresh.operationPlanSha256!==review.operationPlanSha256||fresh.operationPlanSha256!==validation.operationPlanSha256)throw new Error("Jilin cleanup candidate changed after review.");
const publication:CatalogSeedBundle={version:2,generatedAt:approvedAt,handoff:{sourceSystem:"CUAC reviewed Jilin University official school cleanup",cleanedExportName:"catalog.jilin-cleanup.draft.json",cleanedExportSha256:fresh.bundleSha256,sourceSchemaSha256:sha(JSON.stringify({cityFields:[...new Set(candidate.cities!.flatMap((row)=>Object.keys(row)))].sort(),schoolFields:[...new Set(candidate.schools!.flatMap((row)=>Object.keys(row)))].sort()})),reviewReference,prohibitedDataReviewReference:reviewReference,approvalRecordedAt:approvedAt,sourceReadOnly:true,prohibitedDataConfirmedExcluded:true},cities:candidate.cities,schools:candidate.schools!.map((row)=>({...row,status:"active" as const,verificationStatus:"verified" as const,lastVerifiedAt:approvedAt})),programs:[],programIntakes:[],scholarships:[]};const report=createCatalogMigrationValidationReport(publication);if(!report.ok||report.summary.schools!==1||report.summary.programs!==0||report.summary.scholarships!==0)throw new Error(`Invalid Jilin cleanup publication: ${report.errors.join(" ")}`);
const legacyProgramIds:string[]=review.reconciliation.databaseBaseline.legacyProgramIds,legacyScholarshipIds:string[]=review.reconciliation.databaseBaseline.legacyScholarshipIds,protectedProgramIds:string[]=review.reconciliation.databaseBaseline.protectedProgramIds,protectedScholarshipIds:string[]=review.reconciliation.databaseBaseline.protectedScholarshipIds,archivedProgramSlugs:string[]=review.reconciliation.archives.filter((row:any)=>row.entityType==="program").map((row:any)=>row.slug),archivedScholarshipSlugs:string[]=review.reconciliation.archives.filter((row:any)=>row.entityType==="scholarship").map((row:any)=>row.slug),approval={version:1,approvedAt,reviewReference,approvedReviewSha256:approvedHash,publicationBundle:"seeds/catalog.jilin-cleanup.approved.local.json",publicationBundleSha256:report.bundleSha256,schoolSlug:"jilin-university",archivedProgramSlugs,archivedScholarshipSlugs,physicalDeletion:false,prohibitedDataConfirmedExcluded:true};async function writeOrVerify(path:string,value:unknown){const body=`${JSON.stringify(value,null,2)}\n`;try{await writeFile(path,body,{encoding:"utf8",flag:"wx"});}catch(error:any){if(error?.code!=="EEXIST")throw error;if(await readFile(path,"utf8")!==body)throw new Error(`Existing artifact differs: ${path}`);}}await writeOrVerify(paths.output,publication);await writeOrVerify(paths.approval,approval);
const target=assertLocalCatalogPublishTarget(state,publication,confirmation,reviewReference),pool=createPostgresPool({databaseUrl:target.databaseUrl,max:1,applicationName:"cuac:jilin-cleanup-publish"});try{const client=createTransactionalSqlClient(pool);const result=await client.transaction(async(tx)=>{const school=await tx.query<{id:string;status:string;verification_status:string}>("select id,status,verification_status from schools where slug='jilin-university'",[]);if(school.length!==1||school[0].id!==review.reconciliation.databaseBaseline.schoolId||school[0].status!=="active"||!["unverified","verified"].includes(school[0].verification_status))throw new Error("Jilin school baseline changed.");const protectedPrograms=await tx.query<{status:string;verification_status:string}>("select status,verification_status from programs where id=any($1::uuid[])",[protectedProgramIds]),protectedScholarships=await tx.query<{status:string;verification_status:string}>("select status,verification_status from scholarships where id=any($1::uuid[])",[protectedScholarshipIds]),legacyPrograms=await tx.query<{status:string;verification_status:string}>("select status,verification_status from programs where id=any($1::uuid[])",[legacyProgramIds]),legacyScholarships=await tx.query<{status:string;verification_status:string}>("select status,verification_status from scholarships where id=any($1::uuid[])",[legacyScholarshipIds]);if(protectedPrograms.length!==244||protectedPrograms.some((row)=>row.status!=="active"||row.verification_status!=="verified")||protectedScholarships.length!==6||protectedScholarships.some((row)=>row.status!=="active"||row.verification_status!=="verified")||legacyPrograms.length!==3||legacyPrograms.some((row)=>!["active","archived"].includes(row.status)||row.verification_status!=="unverified")||legacyScholarships.length!==1||legacyScholarships.some((row)=>!["active","archived"].includes(row.status)||row.verification_status!=="unverified"))throw new Error("Jilin reconciliation baseline changed.");const written=await new CatalogSeedWriter(tx).writeBundle(publication);if(!written.ok)throw new Error(`Catalog write failed: ${written.errors.join(" ")}`);await tx.query("update programs set status='archived',updated_at=now() where id=any($1::uuid[]) and status='active'",[legacyProgramIds]);await tx.query("update scholarships set status='archived',updated_at=now() where id=any($1::uuid[]) and status='active'",[legacyScholarshipIds]);const final=await tx.query<{school_verified:string;programs:string;verified_programs:string;scholarships:string;verified_scholarships:string;program_archives:string;scholarship_archives:string}>("select (select count(*) from schools where id=$1 and status='active' and verification_status='verified' and last_verified_at=$4)::text school_verified,(select count(*) from programs where school_id=$1 and status='active')::text programs,(select count(*) from programs where school_id=$1 and status='active' and verification_status='verified')::text verified_programs,(select count(*) from scholarships where school_id=$1 and status='active')::text scholarships,(select count(*) from scholarships where school_id=$1 and status='active' and verification_status='verified')::text verified_scholarships,(select count(*) from programs where id=any($2::uuid[]) and status='archived')::text program_archives,(select count(*) from scholarships where id=any($3::uuid[]) and status='archived')::text scholarship_archives",[school[0].id,legacyProgramIds,legacyScholarshipIds,approvedAt]);if(final[0]?.school_verified!=="1"||final[0]?.programs!=="244"||final[0]?.verified_programs!=="244"||final[0]?.scholarships!=="6"||final[0]?.verified_scholarships!=="6"||final[0]?.program_archives!=="3"||final[0]?.scholarship_archives!=="1")throw new Error(`Jilin cleanup post-check failed: ${JSON.stringify(final[0])}`);return{written,...final[0]};});console.log(JSON.stringify({ok:true,target:target.publicTarget,approvedReviewSha256:approvedHash,publicationBundleSha256:report.bundleSha256,approvalPath:paths.approval,outputPath:paths.output,...result},null,2));}finally{await pool.end();}

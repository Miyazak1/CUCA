import { readFile } from "node:fs/promises";
import { createPostgresPool } from "../src/server/db/postgres-client.ts";
import { localDatabaseUrl } from "./lib/local-development.ts";
const names=["Beijing Institute of Technology","South China University of Technology","Southeast University","Tongji University","Lanzhou University","Central South University","University of Chinese Academy of Sciences","Beijing University of Posts and Telecommunications","China University of Geosciences Wuhan","China University of Petroleum Beijing","Hunan University","Northeastern University","Northwest A&F University","East China University of Science and Technology","Nanjing University of Aeronautics and Astronautics"];
const state=JSON.parse(await readFile(".cuac-local/runtime.json","utf8")),pool=createPostgresPool({databaseUrl:localDatabaseUrl(state),max:1,applicationName:"cuac:school-presence"});try{const rows=await pool.query("select name_en,slug,verification_status from schools where lower(name_en)=any($1::text[]) order by name_en",[names.map((v)=>v.toLowerCase())]);console.log(JSON.stringify({present:rows.rows,absent:names.filter((name)=>!rows.rows.some((row:any)=>row.name_en.toLowerCase()===name.toLowerCase()))},null,2));}finally{await pool.end();}
